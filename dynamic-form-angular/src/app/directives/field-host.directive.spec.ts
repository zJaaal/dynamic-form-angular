import { FieldHostDirective } from './field-host.directive';

describe('FieldHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldHostDirective();
    expect(directive).toBeTruthy();
  });
});
